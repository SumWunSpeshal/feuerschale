import { NextPage } from "next";
import { Layout } from "src/components/Layout";

const {{pascalCase name}}: NextPage = () => {
  return (
    <Layout authGuarded>
      {{pascalCase name}} page works!
    </Layout>
  );
};

export default {{pascalCase name}};
